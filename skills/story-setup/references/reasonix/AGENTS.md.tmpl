# {项目名} — 网文写作工具集（Reasonix）

本项目已部署 oh-story skills 的 Reasonix 兼容层（skills-only）。

## Skill 路由表

Reasonix 会从项目 `.agents/skills`（部署时创建的指向 `skills/` 的 symlink）发现 `SKILL.md`；也可用根 `reasonix-plugin.json` 走 `reasonix plugin install`。优先通过 Reasonix 的 skill 入口调用；若当前客户端未显示直接命令，可用自然语言点名 skill。

| 意图 | Skill | 说明 |
|------|-------|------|
| 写长篇 / 开书 / 续写 | story-long-write | 长篇网文写作（逐章推进） |
| 写短篇 | story-short-write | 短篇网文写作（情绪驱动） |
| 长篇拆文 | story-long-analyze | 长篇小说深度拆解 |
| 短篇拆文 | story-short-analyze | 短篇小说拆文分析 |
| 长篇扫榜 | story-long-scan | 长篇小说榜单与市场趋势 |
| story-data-analyze、数据分析、后台数据 | story-data-analyze | 后台数据归因分析，三层定位读者流失 |
| 短篇扫榜 | story-short-scan | 短篇小说榜单与情绪风口 |
| 去 AI 味 | story-deslop | 去除 AI 写作痕迹 |
| 封面 | story-cover | 生成封面图；需要 `GPT_IMAGE_API_KEY` |
| 审查 | story-review | 多视角审查；Reasonix 默认 solo/direct fallback |
| 导入 | story-import | 逆向导入已有小说到项目结构 |
| 网文工具箱 | story | 模糊意图自动分发 |
| 准备写书 / 部署 | story-setup | 项目结构与 skill 使用入口 |
| 浏览器登录态 / 抓取 | browser-cdp | 浏览器 CDP 工具；需平台允许本地脚本/浏览器控制 |

## Reasonix 兼容说明

- 只部署 skills 与本 `AGENTS.md`：Reasonix 原生扫描 `.agents/skills`（部署时创建的指向 `skills/` 的 symlink）发现 `skills/*/SKILL.md`，并把 `AGENTS.md` 作为项目指令加载。也可用根 `reasonix-plugin.json` 走 `reasonix plugin install`；`reasonix doctor capabilities` 校验 skill 发现。
- 暂不部署 Reasonix hooks/custom agents：写正文前大纲守卫、commit 提醒、session/compact 自动注入在 Reasonix 下不是硬拦截，只作为 skill 内软约束执行。
- 涉及专业 Agent 的 Skill（story-review 多视角、story-long-write 的 narrative-writer/story-architect 等）按 solo/direct fallback 执行，并在结果里说明降级。
- Windows 未启用 symlink 时改走根 `reasonix-plugin.json` 原生 plugin 安装。
- `story-cover` 依赖 `GPT_IMAGE_API_KEY`，未配置时该 skill 不可用。

## 文件结构

- `skills/` — 项目本地 skills，包含 oh-story 的 14 个 skill
- `拆文库/` — 拆文分析结果存放目录
- `{书名}/正文/` — 长篇小说正文章节
- `{书名}/正文.md` — 短篇小说正文
- `{书名}/设定/` — 角色设定、世界设定
- `{书名}/大纲/` — 卷纲、细纲
- `{书名}/追踪/` — `_tracking-state.json` 唯一结构化权威、固定 7 栏 `上下文.md` 续写状态卡、逐章紧凑记录、核心角色独立派生快照、伏笔当前视图、作者与读者双时间线；全部通过追踪工具生成
- `{书名}/对标/` — 对标作品分析

## 写作约束

- 写正文前必须先有对应大纲：长篇 `大纲/细纲_第N章*.md`，短篇 `小节大纲.md`。
- 中文长篇/短篇正文全程锁定中文；未获用户/设定明确授权的英文句段、裸英文词和英文对话必须改成中文。缩写/型号、URL/邮箱/文件名/代码和 `.deslop-whitelist` 精确登记项除外。英文/海外意图优先路由 `story-globalize`；未安装时报告缺失，不进入中文写作流程。
- Reasonix 未部署硬 hook 拦截；执行写作 skill 时必须由 skill 流程自行检查大纲、上下文和追踪文件，不得跳过。
- 无 custom agents 时按 solo/direct 执行；遇到 skill 要求调用 story-architect、narrative-writer 等 agent 时，改由当前 Agent 直接完成，并在结果里说明降级。
- **去AI味/英文泄漏自锁**（本平台无 hook 兜底，此条是唯一防线）：每章正文落盘后，同一轮内立即运行 `check-ai-patterns.js --check --fail-on=blocking` 与 `check-degeneration.js --check --language=zh --fail-on=blocking`，将毒句式、退化信号和未授权英文句段/裸词清零；写下一章前先复查上一章无欠账。`<!-- 去味:跳过 -->` 不豁免英文泄漏；确需保留的外语写入 `.deslop-whitelist`。
- Compact / 新会话后优先读取 `{书名}/追踪/上下文.md` 恢复当前写作状态。

## Compact 后恢复上下文

写作中的关键上下文：
1. 当前写作项目名称和进度
2. 最近讨论的角色设定变更
3. 未完成的伏笔列表
4. 当前章节的情绪/节奏目标

如果存在 `{书名}/追踪/上下文.md`，compact 后首先读取该文件恢复上下文。

## 分支推演

用户要求“分支推演 / 路线比较 / 推演几个走向”时，使用 `story-long-write` 的分支推演流程。推演产物放在 `大纲/推演/`，未经用户明确选择和另行授权，不得写入正式大纲或追踪状态。

## 中文正文英文零容忍

中文正文和台词中出现未授权拉丁字母词时一律 blocking，包括短词、专名、缩写和中英混合台词。只豁免 URL、邮箱、文件路径、代码等非正文结构以及 `.deslop-whitelist` 精确登记项；语言泄漏未清零不得交付。

## 适度对白技巧与验收

重要对白场景先读取 `skills/story-deslop/references/dialogue-craft-moderate.md`，形成轻量对白卡。正文自然混用无标签对白、简单“说/问”、有效动作和叙述反应，不追求零标签或句句潜台词。中文语言 Gate 后运行 `node skills/story-deslop/scripts/dialogue_drift_gate.js --current <正文文件> --history-dir <正文目录>`；明确机械阻断项退回，密度与动词集中预警进入语义审查。机械与语义验收通过前禁止追踪提交和下一章。
